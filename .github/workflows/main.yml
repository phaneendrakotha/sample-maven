name: Java CI

on: [push]

jobs:
  build:
    defaults:
      run:
        working-directory: myapp
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build with Maven
        run: mvn clean install -file ./my-app
